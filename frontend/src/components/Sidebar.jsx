import React from 'react';

/**
 * Sidebar Component
 * Navigation menu for authenticated users
 * Displays different menu items based on user role
 * 
 * Structure:
 * - Dashboard
 * - Medicines
 * - Batches
 * - Purchases
 * - Sales
 * - Reports
 * - AI Assistant
 * - User Profile
 * - Logout
 */
const Sidebar = () => {
  return (
    <aside className="sidebar">
      {/* Navigation will be implemented with links */}
    </aside>
  );
};

export default Sidebar;
